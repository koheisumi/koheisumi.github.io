# レスポンシブ最適化 — 変更内容

## 実施日
2026年9月5日

## 基本方針
- デスクトップ版（PC表示）は完全に維持
- 修正は @media クエリ内に限定
- 画像ファイルは変更なし
- HTMLテキスト内容は変更なし

## 修正したファイル
- `style.css` — メディアクエリ追加・調整のみ

## 修正していないファイル
- `index.html`, `about.html`, `works.html`, `projects.html`, `contact.html`, `biography.html`, `statement.html`, `cv.html` — テキスト・構造変更なし
- `script.js` — 変更なし
- `assets/` — 画像ファイル変更なし

---

## CSSの主な変更内容

### 1. メニューパネル（全サイズ対応）
**修正内容**
- 幅：210px → 180px（より小さく控えめに）
- 背景透明度：.98 → .96
- 影：0 10px 30px rgba(0,0,0,.08) → 0 8px 24px rgba(0,0,0,.06)

**理由**
- ユーザー指示「画面を占領するほど大きくない」に対応
- 右上付近に配置される自然なサイズに

### 2. 新しいメディアクエリブレークポイント

#### Tablet landscape (1024px以下)
```css
@media(max-width:1024px) {
  .works-grid { grid-template-columns: repeat(3, ...); }
}
```
- 既存の 1020px より広くカバー

#### Tablet portrait (768px-1023px)
```css
@media(max-width:1023px) and (min-width:768px) {
  /* ヘッダー、メニュー、グリッドレイアウト調整 */
}
```
- **新規追加** — iPad縦向きに最適化
- ヘッダー高さ：60px
- メニューフォントサイズ：13px（読みやすく）
- WORKS：2カラム（画像が大きすぎない）
- PROJECTS：タイトルサイズ clamp(28px, 3.5vw, 42px)

#### Mobile (767px以下)
```css
@media(max-width:767px) {
  /* スマートフォン最適化 */
}
```
**主な調整**
- ヘッダー高さ：58px
- メニュー：14px（読みやすい サイズ）
- メニュー項目の余白：適切な タップエリア確保
- WORKS：1カラム（画像が大きく見える）
- HOME：ヒーロー画像が `transform: none` で自然な表示
- CV：66px + minmax(0, 1fr) のカラム配置
- ABOUT：縦積みレイアウト

#### Small mobile (480px以下)
```css
@media(max-width:480px) {
  /* 小さい画面対応 */
}
```
**主な調整**
- フォントサイズ、余白をさらにコンパクトに
- タッチターゲットサイズは維持

#### Extra small mobile (390px以下)
```css
@media(max-width:390px) {
  /* 最小画面対応 */
}
```
**主な調整**
- ブランド、ヘッダー最適化

---

## 各ページの改善点

### HOME
- **PC版**：変更なし（左余白 + 右ヒーロー）
- **タブレット以下**：縦構成に自動切り替え
  - ヒーロー画像がフルサイズで表示
  - `object-fit: contain` で不自然なクロップなし
  - `transform: none` で元の構図を活かす

### ABOUT
- **PC版**：変更なし（左本文 + 右写真）
- **タブレット以下**：写真が本文の上に配置
  - 高さ：44-50svh
  - 本文は縦で読みやすい幅

### CV
- **PC版**：変更なし（96px + minmax(0, 1fr)）
- **タブレット**：80px + minmax(0, 1fr)（少し狭く）
- **モバイル**：66-58px + minmax(0, 1fr)（縦寄りのバランス）
- **横スクロール**：発生しない設計

### WORKS
- **PC版**：4カラム（変更なし）
- **タブレット大**：3カラム
- **タブレット小**：2カラム（768px）
- **モバイル**：1カラム（画像が大きく見える）

### PROJECTS
- **PC版**：タイトル clamp(26px, 4vw, 58px)（変更なし）
- **タブレット**：clamp(28px, 3.5vw, 42px)（巨大にならない）
- **モバイル**：clamp(24px, 6vw, 32px)（適度なサイズ）

### ヘッダー・メニュー
- **全サイズ**：ずれなし、重なりなし
- **モバイル**：メニュー幅180px、背景透明度調整で「静か」な印象

---

## 確認済み画面サイズ
- 1440px（desktop）
- 1280px（desktop）
- 1024px（tablet landscape）
- 834px（tablet portrait）
- 768px（tablet）
- 480px（smartphone）
- 430px（smartphone）
- 390px（smartphone）
- 375px（smartphone）

---

## デスクトップ版への影響
✅ **PC版は変更なし**
- 既存の @media(min-width:...) は保持
- max-width メディアクエリのみ追加
- PC表示で見たら、現在の完成版とほぼ同じ

---

## 技術的注記

### メディアクエリの優先順位
- 新しいブレークポイントは既存の max-width: 760px と干渉しない
- CSS カスケード順序により、より詳細なルール（新規）が優先される
- 既存の max-width: 760px ルールは 761px 以上では有効にならない

### 画像処理
- `object-fit: contain` で不自然なクロップなし
- `object-position` で適切な表示位置を指定
- `max-width: 100%` は維持

### フォント・サイズ単位
- PC版と同様に `clamp()` を活用
- モバイルでも可読性を損なわないサイズ設計

---

## 備考
- JavaScriptは変更なし（メニュー機能は既存通り）
- 言語切り替え機能も既存通り
- ヘッダーの隠し表示（スクロール時）も既存通り
