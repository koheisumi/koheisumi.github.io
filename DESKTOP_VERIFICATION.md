# デスクトップ版への影響確認

## ✅ PC版（1200px以上）での動作確認

### ルール
- PC版のレスポンシブルールはすべて @media(min-width:...) で定義
- 新規追加ルールは @media(max-width:...) のみ
- PC表示では新規ルールは一切適用されない

### 確認方法
CSS内で以下のルールを検証：

1. **既存の PC ルール（保持）**
   ```css
   @media(min-width:761px) { ... }
   @media(min-width:1101px) { ... }
   @media(max-width:1020px) { ... }  ← 1021px以上は影響なし
   @media(max-width:1024px) { ... }  ← 1025px以上は影響なし
   ```

2. **新規追加ルール（PC表示では無視）**
   ```css
   @media(max-width:1024px) { ... }     ← 1025px以上は適用されない
   @media(max-width:1023px) and (min-width:768px) { ... } ← PC表示では無視
   @media(max-width:767px) { ... }      ← PC表示では無視
   @media(max-width:480px) { ... }      ← PC表示では無視
   @media(max-width:390px) { ... }      ← PC表示では無視
   ```

### PC表示で確認すべき要素
✅ ヘッダー：固定位置、スタイル変更なし
✅ ブランド（KOHEI SUMI）：フォントサイズ変更なし
✅ 言語切り替え：配置・サイズ変更なし
✅ ハンバーガーメニュー：サイズ変更なし
✅ メニューパネル：幅180px（※ 唯一の変更、PC版での視認性に悪影響なし）
✅ HOME：左余白 + 右ヒーロー画像（グリッド分割維持）
✅ ABOUT：左本文 + 右写真（grid-template-columns 維持）
✅ WORKS：4カラムグリッド（変更なし）
✅ PROJECTS：3カラムグリッド（タイトル左、メタ中央、矢印右）
✅ CV：2カラム（年 + 内容）、テキスト変更なし
✅ フッター：フォント・余白変更なし

---

## CSSカスケード検証

### PC版の最終的なレイアウト計算
1. デフォルトルール適用
2. @media(min-width:761px) 適用
3. @media(min-width:1101px) 適用
4. @media(max-width:1020px) 適用 ← ※ 1020px以下の場合のみ
5. **@media(max-width:1024px) 非適用** ← PC版では 1025px以上
6. **@media(max-width:1023px) ... 非適用** ← PC版では 1024px以上
7. **@media(max-width:767px) 以下 全て非適用** ← PC版では該当しない

### PC表示で影響を受けるのは
✅ メニューパネルのみ（幅180px、影の調整）
   - 既存の .menu-panel { width: min(210px, calc(100vw - 36px)); }
   - 新規設定：width: min(180px, calc(100vw - 48px));
   - **理由**: PC表示でも右上にメニューが表示されるため、より控えめなサイズが適切

❌ 影響を受けないもの
   - ページレイアウト
   - グリッドシステム
   - タイポグラフィ（見出し、本文）
   - 余白（padding, margin）
   - 色、背景

---

## 推奨PC確認環境
- ブラウザ：Chrome, Firefox, Safari（最新版）
- 表示サイズ：1440px, 1280px, 1200px, 1024px
- 各ページ：TOP, WORKS, PROJECTS, ABOUT（CV含む）, CONTACT

---

## チェックリスト（PC版確認）
- [ ] 1440px で見たら、現在の完成版と変わらない
- [ ] 1280px で見たら、現在の完成版と変わらない
- [ ] メニューが小さく見えるが、機能・配置は同じ
- [ ] ヘッダーがずれていない
- [ ] ABOUT の左右レイアウトが維持されている
- [ ] WORKS の4カラムが維持されている
- [ ] PROJECTS のタイトル・メタ・矢印の配置が維持されている
- [ ] CV の2カラムレイアウトが維持されている
- [ ] フッターの表示に変更がない

